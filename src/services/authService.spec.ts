import { describe, it, expect } from 'vitest';
import { authService } from './authService';

describe('authService', () => {
    it('should have a placeholder test', () => {
        // TODO: add real tests for authService functions
        expect(true).toBe(true);
    });
});
